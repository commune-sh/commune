{
    "name": "commune",
    "version": "1.0.0",
    "private": true,
    "scripts": {
        "dev": "npm run dev --workspace=web",
        "build": "npm run build --workspace=web",
        "desktop": "npm run dev --workspace=desktop",
        "preview": "npm run build --workspace=web"
    },
    "engines": {
        "node": ">=22"
    },
    "workspaces": [
        "packages/*"
    ]
}
